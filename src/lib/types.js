
/**
 * Project Type Definitions (Documentation Only)
 * In JavaScript, these serve as a reference for the data structures used in the app.
 */

// DensityCategory: 'free' | 'moderate' | 'crowded' | 'over-crowded'
// Coordinate: { lat, lng }
// Zone: { id, name, coordinates, capacity, userCount, density, manualDensity, manualDensityAtCount }
// RouteDetails: { route, congestionLevel, alternativeRouteAvailable, alternativeRoute }
// User: { id, name, groupSize, lastLatitude, lastLongitude, lastSeen, lastZoneId, status, role, sos }
// AlertMessage: { id, message, timestamp, zoneId }
